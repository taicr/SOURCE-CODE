#include<bits/stdc++.h>

using namespace std;
#define run() ios_base :: sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
#define ll long long
int main(){
    int t;
    cin>>t;
    while(t--){
        set<char> st;
        string s; cin>>s;
        for(int i=0;i<s.size();i++){
             st.insert(s[i]);
        }
        
    }
}